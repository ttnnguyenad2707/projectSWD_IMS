import { Subject } from "../models"
async function createSubject ({assignment, title, description, status}) {
    try {
        const subject = await Sub
    } catch (error) {
        
    }
}

export default {createSubject}